[Service]
# The helper is best-effort: fingerprint authentication must keep working if
# the optional indicator is unavailable or another process owns the interface.
ExecStartPre=-/usr/local/libexec/elan-led on
ExecStopPost=-/usr/local/libexec/elan-led off
